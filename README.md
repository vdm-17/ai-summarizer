# AI-Summarizer

AI-Summarizer is an utility for summarizing a source content using AI.
Summarizing is performed by a LLM agent, therefore, you will need access
to the LLM API. By default, LLM agent uses the OpenAI API, but any
OpenAI-compatible API is also supported. To change using API just specify
api_base_url in the config.
